# =========================================================
# Unreal Engine + Visual Studio + JetBrains Rider + Xcode
# .gitignore (주석 설명 포함)
# =========================================================

# ---------------------------------------------------------
# Unreal Engine Build / Cache 폴더
# ---------------------------------------------------------
# 빌드 결과물, 임시 파일, 캐시 데이터 등 — 매번 재생성되므로 Git에 불필요

enc_temp_folder*
*.tmp
*.bak


Binaries/
Build/
DerivedDataCache/
Intermediate/
Saved/
Output/
Engine/x64/
Client/x64/

# ---------------------------------------------------------
# IDE별 프로젝트 파일 (자동 생성 / 환경별 다름)
# ---------------------------------------------------------

# Visual Studio
# 프로젝트 메타정보 / 사용자 환경 저장 파일
*.sln
*.suo
*.VC.db
*.VC.opendb
*.opensdf
*.sdf
.vs/
.vsconfig

# JetBrains Rider
# 프로젝트 설정 폴더 (.idea 안에는 개인화된 경로정보가 있음)
.idea/

# Visual Studio Code
# 확장 프로그램, 워크스페이스 관련 설정 (공유 불필요)
.vscode/

# Xcode
# macOS용 IDE — UE가 자동 생성하는 워크스페이스
*.xcodeproj
*.xcworkspace

# ---------------------------------------------------------
# 로그 / 임시 / OS 시스템 파일
# ---------------------------------------------------------
# 운영체제나 프로그램이 자동 생성하는 임시 파일
*.log
*.tmp
.DS_Store        # macOS Finder 캐시
Thumbs.db        # Windows 탐색기 썸네일 캐시

# ---------------------------------------------------------
# 컴파일 산출물 (Object / Library / Executable)
# ---------------------------------------------------------
# 아래 파일들은 소스 빌드 시 자동 생성되는 중간 결과물임

# Object files
*.slo
*.lo
*.o
*.obj

# Precompiled headers
*.gch
*.pch

# Dynamic libraries
*.so
*.dylib
*.dll

# Static libraries
*.lai
*.la
*.a
*.lib

# Executables
*.exe
*.out
*.app
*.ipa

# Fortran module files (드물지만 일부 엔진 플러그인 빌드 시 생성됨)
*.mod
*.smod

# ---------------------------------------------------------
# Unreal Editor 전용 생성 파일
# ---------------------------------------------------------

# 원본 이미지 리소스 (보통 SourceArt 폴더는 참조용 원본만 포함)
SourceArt/**/*.png
SourceArt/**/*.tga

# 맵 빌드시 자동 생성되는 데이터 파일 (저장하지 않아도 됨)
*_BuiltData.uasset

# ---------------------------------------------------------
# Plugins 관련 빌드/중간 파일
# ---------------------------------------------------------
# 플러그인도 엔진과 동일하게 빌드 산출물 제외
Plugins/**/Binaries/*
Plugins/**/Intermediate/*

# ---------------------------------------------------------
# 빌드 아웃풋 (Build/*)
# ---------------------------------------------------------
# 자동 생성되는 빌드 폴더 전체 제외
Build/*
# 단, PakBlacklist 및 아이콘 파일은 예외로 허용
!Build/*/
Build/*/**
!Build/*/PakBlacklist*.txt
!Build/**/*.ico

# ---------------------------------------------------------
# Derived Data Cache (에셋 빌드 캐시)
# ---------------------------------------------------------
# 프로젝트 용량을 많이 차지하지만, 전혀 버전 관리 필요 없음
DerivedDataCache/*

# ---------------------------------------------------------
# 기타 빌드 전처리 관련
# ---------------------------------------------------------
# 일부 툴체인 / Makefile / 빌드 스크립트용 중간 파일
*.d

# =========================================================
#  Summary
# ---------------------------------------------------------
#  버전에 포함되는 항목:
#   - Source/ (소스 코드)
#   - Config/ (설정 파일)
#   - Content/ (에셋 파일)
#   - Plugins/ (단, 코드/리소스만)
#
#  제외되는 항목:
#   - 빌드 산출물 (Binaries, Intermediate, DerivedDataCache 등)
#   - IDE / 환경설정 파일 (.vs, .idea, .vscode)
#   - 캐시 / 로그 / 시스템 파일
# =========================================================
